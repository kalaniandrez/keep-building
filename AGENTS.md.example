# AGENTS.md

Rules and context for any AI agent (Claude Code, Codex, Gemini) working in this repo.
Read this first. This is what lets a different model take over without losing the plot.

## What this project is
<one short paragraph: what it does, the stack, the entry points>

## Hard rules
- <your coding conventions: style, formatting, what to never touch>
- Commit with `checkpoint "<what works>"` after each working change.
- Do not invent priorities. The current task lives in <your task file / issue / board>.

## Where things are
- `<dir>/` -- <what it holds>
- `<dir>/` -- <what it holds>

## How to continue
Run `git log --oneline -8` and `git status` to see recent work, then continue the
current task. Checkpoint often. Leave the tree clean before you stop.
